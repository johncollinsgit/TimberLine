<x-layouts::app.sidebar title="User Agreement"><flux:main><div class="fb-workflow-shell"><header class="fb-workflow-header"><div class="fb-eyebrow">Accepted agreement</div><h1 class="fb-title-xl">{{ $agreement->title }}</h1><p class="fb-subtitle">Version {{ $agreement->currentVersion->version_number }} · accepted {{ $agreement->accepted_at?->toDayDateTimeString() }}</p><div class="mt-4 flex gap-3"><a href="{{ route('agreements.index', ['tenant' => $tenant->slug]) }}" class="fb-btn fb-btn-secondary">All agreements</a><a href="{{ route('agreements.download', ['agreement' => $agreement, 'tenant' => $tenant->slug]) }}" class="fb-btn fb-btn-primary">Download permanent copy</a></div></header><section class="rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm">{!! $agreement->currentVersion->rendered_content !!}</section>@if($agreement->termination)<section class="fb-panel"><div class="fb-panel-body"><h2 class="font-semibold">Termination and export status</h2><p class="mt-2 text-sm text-zinc-600">Effective {{ $agreement->termination->effective_at?->toFormattedDateString() }}. Export window ends {{ $agreement->termination->export_window_ends_at?->toFormattedDateString() }}. Export status: {{ str_replace('_', ' ', $agreement->termination->export_status) }}.</p></div></section>@endif</div></flux:main></x-layouts::app.sidebar>
