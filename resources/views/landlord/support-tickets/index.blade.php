<x-app-layout>
    <x-slot name="header"><div><h1 class="text-xl font-semibold text-zinc-950">Tenant Messages & Tickets</h1><p class="mt-1 text-sm text-zinc-600">One searchable thread per support request, across every workspace.</p></div></x-slot>
    <div class="mx-auto max-w-6xl space-y-5"><section class="rounded-3xl border border-zinc-200 bg-white p-5 shadow-sm"><form class="flex flex-col gap-3 md:flex-row">@foreach(request()->except(['q','status']) as $key => $value)<input type="hidden" name="{{ $key }}" value="{{ $value }}">@endforeach<input name="q" value="{{ $query }}" type="search" autofocus placeholder="Search workspace, owner, subject, or message…" class="min-w-0 flex-1 rounded-2xl border-zinc-300 px-4 py-3"><select name="status" class="rounded-2xl border-zinc-300 px-4 py-3">@foreach(['open'=>'Open & active','all'=>'All tickets','in_progress'=>'In progress','waiting_on_tenant'=>'Waiting on tenant','resolved'=>'Resolved','closed'=>'Closed'] as $key=>$label)<option value="{{ $key }}" @selected($status===$key)>{{ $label }}</option>@endforeach</select><button class="rounded-2xl bg-zinc-950 px-5 py-3 text-sm font-semibold text-white">Search</button></form></section><section class="grid gap-4 lg:grid-cols-2">@forelse($tickets as $ticket)<a href="{{ route('landlord.support-tickets.show', $ticket['id']) }}" class="rounded-3xl border border-zinc-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-emerald-300 hover:shadow-md"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-[.16em] text-emerald-800">{{ $ticket['tenant'] }}</p><h2 class="mt-2 font-semibold text-zinc-950">{{ $ticket['subject'] }}</h2></div><span class="rounded-full bg-zinc-100 px-3 py-1 text-xs font-semibold text-zinc-700">{{ ucfirst(str_replace('_', ' ', $ticket['status'])) }}</span></div><p class="mt-3 text-sm text-zinc-600">{{ $ticket['creator'] ?? 'Workspace user' }} · {{ $ticket['messages_count'] }} messages · {{ ucfirst($ticket['priority']) }}</p><div class="mt-4 flex items-center justify-between text-xs text-zinc-500"><span>{{ $ticket['source_type'] === 'bud_escalation' ? 'Bud escalation' : 'Account Help' }}</span><span>{{ \Carbon\Carbon::parse($ticket['last_activity_at'])->diffForHumans() }} →</span></div></a>@empty<div class="rounded-3xl border border-dashed border-zinc-300 bg-zinc-50 p-8 text-sm text-zinc-600 lg:col-span-2">No tickets match this view.</div>@endforelse</section></div>
</x-app-layout>
