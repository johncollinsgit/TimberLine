<x-app-layout>
    <x-slot name="header"><div class="flex items-center gap-3"><a href="{{ route('landlord.support-tickets.index') }}" class="text-sm font-semibold text-emerald-800">← Tickets</a><span class="text-zinc-300">/</span><h1 class="text-xl font-semibold text-zinc-950">Workspace conversation</h1></div></x-slot>
    @php($ticket = $payload['ticket'])
    <div class="mx-auto grid max-w-6xl gap-6 lg:grid-cols-[1fr_21rem]"><main class="space-y-4"><section class="rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm"><p class="text-xs font-semibold uppercase tracking-[.18em] text-emerald-800">{{ $ticket['source_type'] === 'bud_escalation' ? 'Bud escalation' : 'Account Help' }}</p><h2 class="mt-2 text-2xl font-semibold text-zinc-950">{{ $ticket['subject'] }}</h2><p class="mt-2 text-sm text-zinc-600">{{ $ticket['creator'] ?? 'Workspace user' }} · {{ ucfirst($ticket['priority']) }} priority</p>@if($ticket['resolution_summary'])<div class="mt-5 rounded-2xl bg-emerald-50 p-4 text-sm leading-6 text-emerald-950"><strong>Resolution</strong><p class="mt-1 whitespace-pre-line">{{ $ticket['resolution_summary'] }}</p></div>@endif</section><section class="space-y-3">@foreach($payload['messages'] as $message)<article class="rounded-2xl border p-4 {{ $message['author_context'] === 'landlord' ? 'border-emerald-200 bg-emerald-50/60' : 'border-zinc-200 bg-white' }}"><div class="flex justify-between gap-3 text-xs"><strong>{{ $message['author'] }}</strong><span class="text-zinc-500">{{ \Carbon\Carbon::parse($message['created_at'])->diffForHumans() }}</span></div><p class="mt-2 whitespace-pre-line text-sm leading-6 text-zinc-700">{{ $message['body'] }}</p></article>@endforeach</section><form method="post" action="{{ route('landlord.support-tickets.reply', $ticket['id']) }}" class="rounded-3xl border border-zinc-200 bg-white p-5 shadow-sm">@csrf<textarea name="body" rows="4" required placeholder="Write a clear reply to this workspace…" class="w-full rounded-xl border-zinc-300 text-sm"></textarea><button class="mt-3 rounded-xl bg-zinc-950 px-4 py-2 text-sm font-semibold text-white">Send reply</button></form></main><aside><form method="post" action="{{ route('landlord.support-tickets.update', $ticket['id']) }}" class="sticky top-5 rounded-3xl border border-zinc-200 bg-white p-5 shadow-sm">@csrf @method('PATCH')<h2 class="font-semibold text-zinc-950">Ticket controls</h2><label class="mt-4 block text-xs font-semibold uppercase tracking-wide text-zinc-500">Status<select name="status" class="mt-1 w-full rounded-xl border-zinc-300 text-sm">@foreach(['open'=>'Open','in_progress'=>'In progress','waiting_on_tenant'=>'Waiting on tenant','resolved'=>'Resolved','closed'=>'Closed'] as $key=>$label)<option value="{{ $key }}" @selected($ticket['status']===$key)>{{ $label }}</option>@endforeach</select></label><label class="mt-4 block text-xs font-semibold uppercase tracking-wide text-zinc-500">Priority<select name="priority" class="mt-1 w-full rounded-xl border-zinc-300 text-sm">@foreach(['low','normal','high','urgent'] as $value)<option value="{{ $value }}" @selected($ticket['priority']===$value)>{{ ucfirst($value) }}</option>@endforeach</select></label><label class="mt-4 flex gap-2 text-sm text-zinc-700"><input type="checkbox" name="assign_to_me" value="1" @checked($ticket['assignee'] === auth()->user()?->name) class="rounded border-zinc-300">Assign to me</label><label class="mt-4 block text-xs font-semibold uppercase tracking-wide text-zinc-500">Resolution for tenant<textarea name="resolution_summary" rows="5" class="mt-1 w-full rounded-xl border-zinc-300 text-sm" placeholder="What was fixed or decided?">{{ $ticket['resolution_summary'] }}</textarea></label><button class="mt-4 w-full rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-semibold text-white">Save controls</button></form></aside></div>
</x-app-layout>
