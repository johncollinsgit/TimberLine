<x-app-layout><x-slot name="header"><h1 class="text-xl font-semibold text-zinc-900">Create agreement version</h1></x-slot>
<form method="post" action="{{ route('landlord.agreements.version', $agreement) }}" class="mx-auto max-w-2xl space-y-5 rounded-2xl border border-zinc-200 bg-white p-6">@csrf
    <p class="text-sm text-zinc-600">Changes create a new immutable version. Version {{ $agreement->currentVersion?->version_number }} is never overwritten.</p>
    @php $cards = collect((array) $agreement->currentVersion?->pricing_payload['cards']); $implementation = $cards->firstWhere('key', 'shopify_implementation'); $schedule = (array) ($agreement->currentVersion?->pricing_payload['implementation_payment_schedule'] ?? []); @endphp
    <div class="grid gap-4 sm:grid-cols-3"><label class="text-sm font-medium">Implementation total ($)<input name="implementation_amount" type="number" step="0.01" min="0" value="{{ old('implementation_amount', isset($implementation['amount_cents']) ? $implementation['amount_cents']/100 : '') }}" class="mt-1 block w-full rounded-lg border-zinc-300"></label><label class="text-sm font-medium">Due on acceptance ($)<input name="due_on_acceptance" type="number" step="0.01" min="0" value="{{ old('due_on_acceptance', isset($schedule['due_on_acceptance_cents']) ? $schedule['due_on_acceptance_cents']/100 : '') }}" class="mt-1 block w-full rounded-lg border-zinc-300"></label><label class="text-sm font-medium">Due before launch ($)<input name="due_before_launch" type="number" step="0.01" min="0" value="{{ old('due_before_launch', isset($schedule['due_before_launch_cents']) ? $schedule['due_before_launch_cents']/100 : '') }}" class="mt-1 block w-full rounded-lg border-zinc-300"></label></div>
    <label class="block text-sm font-medium">Additional agreed scope<textarea name="additional_scope" rows="5" maxlength="20000" class="mt-1 block w-full rounded-lg border-zinc-300">{{ old('additional_scope', $agreement->currentVersion?->scope_payload['additional_scope'] ?? '') }}</textarea></label>
    <div class="flex justify-end gap-3"><a href="{{ route('landlord.agreements.show', $agreement) }}" class="rounded-lg border px-4 py-2 text-sm font-semibold">Cancel</a><button class="rounded-lg bg-zinc-950 px-4 py-2 text-sm font-semibold text-white">Create version if changed</button></div>
</form></x-app-layout>
