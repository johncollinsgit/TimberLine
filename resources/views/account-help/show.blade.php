<x-app-layout>
    <x-slot name="header"><div class="flex items-center gap-3"><a href="{{ route('account-help.index') }}" class="text-sm font-semibold text-emerald-800">← Account Help</a><span class="text-zinc-300">/</span><h1 class="text-xl font-semibold text-zinc-950">Conversation</h1></div></x-slot>
    @php($ticket = $payload['ticket'])
    <div class="mx-auto max-w-4xl space-y-5"><section class="rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-[.18em] text-emerald-800">{{ ucfirst(str_replace('_', ' ', $ticket['status'])) }}</p><h2 class="mt-2 text-2xl font-semibold text-zinc-950">{{ $ticket['subject'] }}</h2><p class="mt-2 text-sm text-zinc-600">{{ ucfirst($ticket['category']) }} · {{ ucfirst($ticket['priority']) }} priority</p></div></div>@if($ticket['resolution_summary'])<div class="mt-5 rounded-2xl bg-emerald-50 p-4 text-sm leading-6 text-emerald-950"><strong>Resolution</strong><p class="mt-1 whitespace-pre-line">{{ $ticket['resolution_summary'] }}</p></div>@endif</section><section class="space-y-3">@foreach($payload['messages'] as $message)<article class="rounded-2xl border p-4 {{ $message['author_context'] === 'landlord' ? 'border-emerald-200 bg-emerald-50/60' : 'border-zinc-200 bg-white' }}"><div class="flex justify-between gap-3 text-xs"><strong class="text-zinc-900">{{ $message['author'] }}</strong><span class="text-zinc-500">{{ \Carbon\Carbon::parse($message['created_at'])->diffForHumans() }}</span></div><p class="mt-2 whitespace-pre-line text-sm leading-6 text-zinc-700">{{ $message['body'] }}</p></article>@endforeach</section><form method="post" action="{{ route('account-help.reply', $ticket['id']) }}" class="rounded-3xl border border-zinc-200 bg-white p-5 shadow-sm">@csrf<textarea name="body" required rows="4" placeholder="Add a reply or more context…" class="w-full rounded-xl border-zinc-300 text-sm"></textarea><button class="mt-3 rounded-xl bg-zinc-950 px-4 py-2 text-sm font-semibold text-white">Reply</button></form></div>
</x-app-layout>
