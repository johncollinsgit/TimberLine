<x-layouts::app.sidebar :title="str($screen)->headline()">
    <flux:main>
        <div class="mx-auto max-w-[1440px] space-y-5 pb-10">
            @if(session('status'))<div class="border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-950" role="status">{{ session('status') }}</div>@endif
            @if($errors->any())<div class="border border-rose-200 bg-rose-50 px-4 py-3 text-sm font-medium text-rose-950" role="alert">{{ $errors->first() }}</div>@endif

            <x-ui.operational-header :title="str($screen)->headline()" :back="['href' => route('managed-website.index'), 'label' => 'Website']" :description="$screen === 'orders' ? 'Native Website Commerce only. Shopify and legacy orders remain separate.' : ($screen === 'customers' ? 'Website customers are separate from your marketing and provider records.' : 'Products and services used by your native Website storefront.')">
                @if($screen === 'orders')<a class="fb-btn fb-btn-primary" href="{{ route('managed-website.orders.create') }}">Create draft order</a>
                @elseif($screen === 'customers')<a class="fb-btn fb-btn-primary" href="{{ route('managed-website.customers.create') }}">Add customer</a>
                @else
                    <a class="fb-btn fb-btn-secondary" href="{{ route('managed-website.products.export') }}">Export</a>
                    @if($importsEnabled ?? false)<a class="fb-btn fb-btn-secondary" href="{{ route('managed-website.commerce.imports') }}">Connected imports</a>@endif
                    @if($shippingEnabled ?? false)<a class="fb-btn fb-btn-secondary" href="{{ route('managed-website.commerce.shipping') }}">Shipping</a>@endif
                    @if($isEditorEnabled)<a class="fb-btn fb-btn-primary" href="#new-product">Add product</a>@endif
                @endif
            </x-ui.operational-header>

            @if($screen === 'products')
                @if($isEditorEnabled)
                    <details class="border border-zinc-200 bg-zinc-50">
                        <summary class="cursor-pointer px-4 py-3 text-sm font-semibold text-zinc-800">Import an Everbranch product CSV</summary>
                        <form method="POST" action="{{ route('managed-website.products.import') }}" enctype="multipart/form-data" class="flex flex-wrap items-end gap-3 border-t border-zinc-200 p-4">@csrf
                            <label class="text-sm font-medium text-zinc-700">Catalog CSV<input required name="catalog" type="file" accept=".csv,text/csv" class="mt-1 block text-sm"></label>
                            <button class="fb-btn fb-btn-secondary" type="submit">Import catalog</button>
                            <p class="text-xs text-zinc-500">Matching handles update; invalid files roll back completely.</p>
                        </form>
                    </details>
                @endif
                <section class="overflow-hidden border border-zinc-200 bg-white">
                    <x-ui.operational-toolbar><div class="text-sm font-semibold text-zinc-900">All products <span class="ml-1 font-normal text-zinc-500">{{ $products->total() }} total</span></div><span class="text-xs text-zinc-500">Catalog changes stay in Website Commerce.</span></x-ui.operational-toolbar>
                    <div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b border-zinc-200 bg-zinc-50 text-xs font-medium text-zinc-500"><tr><th class="px-4 py-3">Product</th><th class="px-4 py-3">Type</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Inventory</th><th class="px-4 py-3 text-right">Retail</th><th class="px-4 py-3 text-right">Wholesale</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse($products as $product)@php($variant = $product->variants->first())<tr class="hover:bg-zinc-50"><td class="px-4 py-3"><span class="font-medium text-zinc-950">{{ $product->title }}</span><span class="block text-xs text-zinc-500">{{ $product->handle }}</span></td><td class="px-4 py-3 text-zinc-700">{{ str($product->product_type)->headline() }}</td><td class="px-4 py-3"><x-ui.status-badge :tone="$product->status === 'active' ? 'success' : 'neutral'">{{ str($product->status)->headline() }}</x-ui.status-badge></td><td class="px-4 py-3 text-zinc-700">{{ $product->track_inventory ? ($variant?->inventory_quantity ?? 0) : 'Not tracked' }}</td><td class="px-4 py-3 text-right font-medium text-zinc-900">${{ number_format(($variant?->price_cents ?? 0) / 100, 2) }}</td><td class="px-4 py-3 text-right text-zinc-700">{{ $variant?->wholesale_price_cents === null ? '—' : '$'.number_format($variant->wholesale_price_cents / 100, 2) }}</td><td class="px-4 py-3 text-right">@if($isEditorEnabled)<details class="text-left"><summary class="cursor-pointer text-xs font-semibold text-emerald-800 hover:underline">Edit</summary><form method="POST" action="{{ route('managed-website.products.update', $product) }}" class="mt-3 grid min-w-72 gap-2 border-l border-zinc-200 pl-3">@csrf @method('PUT')<input required name="title" value="{{ $product->title }}" class="rounded-md border-zinc-300 text-xs" aria-label="Product title"><select name="product_type" class="rounded-md border-zinc-300 text-xs" aria-label="Product type"><option value="physical" @selected($product->product_type === 'physical')>Physical</option><option value="service" @selected($product->product_type === 'service')>Service</option><option value="quote" @selected($product->product_type === 'quote')>Quote only</option></select><div class="grid grid-cols-2 gap-2"><input required name="price" type="number" step=".01" min="0" value="{{ number_format(($variant?->price_cents ?? 0)/100, 2, '.', '') }}" class="rounded-md border-zinc-300 text-xs" aria-label="Retail price"><input name="wholesale_price" type="number" step=".01" min="0" value="{{ $variant?->wholesale_price_cents === null ? '' : number_format($variant->wholesale_price_cents/100, 2, '.', '') }}" class="rounded-md border-zinc-300 text-xs" placeholder="Wholesale price" aria-label="Wholesale price"></div><input name="sku" value="{{ $variant?->sku }}" class="rounded-md border-zinc-300 text-xs" placeholder="SKU"><input name="image_url" value="{{ data_get($product->media, '0') }}" class="rounded-md border-zinc-300 text-xs" placeholder="https:// product image"><select name="status" class="rounded-md border-zinc-300 text-xs" aria-label="Product status"><option value="draft" @selected($product->status === 'draft')>Draft</option><option value="active" @selected($product->status === 'active')>Active</option><option value="archived" @selected($product->status === 'archived')>Archived</option></select><textarea name="description" class="rounded-md border-zinc-300 text-xs" aria-label="Description">{{ $product->description }}</textarea><input type="hidden" name="track_inventory" value="{{ $product->track_inventory ? 1 : 0 }}"><input type="hidden" name="inventory_quantity" value="{{ $variant?->inventory_quantity ?? 0 }}"><input type="hidden" name="is_available" value="{{ $variant?->is_available ? 1 : 0 }}"><button class="fb-btn fb-btn-secondary justify-center text-xs" type="submit">Save changes</button></form>@if($product->status !== 'archived')<form method="POST" action="{{ route('managed-website.products.destroy', $product) }}" class="mt-2" onsubmit="return confirm('Archive this product? Existing order history remains intact.')">@csrf @method('DELETE')<button class="text-xs font-semibold text-rose-700 hover:underline" type="submit">Archive product</button></form>@endif</details>@endif</td></tr>@empty<tr><td colspan="7"><x-ui.operational-empty-state title="No Website products yet" description="Add a product or import an Everbranch catalog when this branch is ready." /></td></tr>@endforelse</tbody></table></div>
                </section>
                @if($isEditorEnabled)<section id="new-product" class="border border-zinc-200 bg-white"><div class="border-b border-zinc-200 px-4 py-3"><h2 class="text-sm font-semibold text-zinc-950">Add product</h2><p class="mt-1 text-xs text-zinc-500">Saved as a native Website Commerce product.</p></div><form method="POST" action="{{ route('managed-website.products.store') }}" class="grid gap-4 p-4 md:grid-cols-2">@csrf<label class="text-sm font-medium">Title<input required name="title" class="mt-1 block w-full rounded-md border-zinc-300 text-sm" placeholder="Five-stave chair"></label><label class="text-sm font-medium">Sell as<select name="product_type" class="mt-1 block w-full rounded-md border-zinc-300 text-sm"><option value="physical">Physical product</option><option value="service">Fixed-price service</option><option value="quote">Quote-only service</option></select></label><label class="text-sm font-medium">Retail price<input required name="price" type="number" min="0" step=".01" class="mt-1 block w-full rounded-md border-zinc-300 text-sm" value="0.00"></label><label class="text-sm font-medium">Wholesale price<input name="wholesale_price" type="number" min="0" step=".01" class="mt-1 block w-full rounded-md border-zinc-300 text-sm" placeholder="Optional"></label><label class="text-sm font-medium">SKU<input name="sku" class="mt-1 block w-full rounded-md border-zinc-300 text-sm"></label><label class="text-sm font-medium">Status<select name="status" class="mt-1 block w-full rounded-md border-zinc-300 text-sm"><option value="draft">Draft</option><option value="active">Active</option></select></label><label class="text-sm font-medium md:col-span-2">Product image URL<input name="image_url" type="url" class="mt-1 block w-full rounded-md border-zinc-300 text-sm" placeholder="https://"></label><label class="text-sm font-medium md:col-span-2">Description<textarea name="description" rows="3" class="mt-1 block w-full rounded-md border-zinc-300 text-sm"></textarea></label><label class="flex items-center gap-2 text-sm font-medium"><input type="hidden" name="track_inventory" value="0"><input type="checkbox" name="track_inventory" value="1"> Track inventory</label><label class="text-sm font-medium">Starting inventory<input name="inventory_quantity" type="number" min="0" class="mt-1 block w-full rounded-md border-zinc-300 text-sm" value="0"></label><input type="hidden" name="is_available" value="1"><div class="md:col-span-2"><button class="fb-btn fb-btn-primary" type="submit">Save product</button></div></form></section>@endif

            @elseif($screen === 'customers')
                <section class="overflow-hidden border border-zinc-200 bg-white">
                    <x-ui.operational-toolbar><form class="flex w-full max-w-md gap-2" method="GET" action="{{ route('managed-website.customers.index') }}"><label class="flex-1 text-sm text-zinc-600">Search customers<input name="q" type="search" value="{{ $customerQuery ?? '' }}" class="mt-1 block w-full rounded-md border-zinc-300 text-sm" placeholder="Name, email, or phone"></label><button class="fb-btn fb-btn-secondary mt-6" type="submit">Search</button></form><span class="text-xs text-zinc-500">{{ $customers->total() }} customers</span></x-ui.operational-toolbar>
                    <div class="overflow-x-auto"><table id="website-customers" class="min-w-full text-left text-sm"><thead class="border-b border-zinc-200 bg-zinc-50 text-xs font-medium text-zinc-500"><tr><th class="px-4 py-3">Customer</th><th class="px-4 py-3">Email</th><th class="px-4 py-3">Phone</th><th class="px-4 py-3">Created</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse($customers as $customer)<tr class="hover:bg-zinc-50"><td class="px-4 py-3 font-medium text-zinc-950">{{ trim($customer->first_name.' '.$customer->last_name) ?: 'Website shopper' }}</td><td class="px-4 py-3 text-zinc-700">{{ $customer->email ?: '—' }}</td><td class="px-4 py-3 text-zinc-700">{{ $customer->phone ?: '—' }}</td><td class="px-4 py-3 text-zinc-500">{{ $customer->created_at->format('M j, Y') }}</td><td class="px-4 py-3 text-right"><a class="text-sm font-medium text-emerald-800 hover:underline" href="{{ route('managed-website.customers.show', $customer) }}">Open</a></td></tr>@empty<tr><td colspan="5"><x-ui.operational-empty-state title="No Website customers yet" description="Customers created here stay separate from marketing and connected platforms." action-label="Add customer" :action-href="route('managed-website.customers.create')" /></td></tr>@endforelse</tbody></table></div>
                    <div class="border-t border-zinc-200 px-4 py-3">{{ $customers->links() }}</div>
                </section>

            @else
                <section class="overflow-hidden border border-zinc-200 bg-white">
                    <x-ui.operational-toolbar><form class="flex flex-wrap items-end gap-2" method="GET" action="{{ route('managed-website.orders.index') }}"><label class="text-xs font-medium text-zinc-600">Search order<input name="q" value="{{ data_get($filters ?? [], 'q') }}" class="mt-1 block w-36 rounded-md border-zinc-300 text-sm" placeholder="#1001"></label><label class="text-xs font-medium text-zinc-600">Payment<select name="payment" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">All</option>@foreach(['pending','paid','failed','partially_refunded','refunded'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'payment') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label><label class="text-xs font-medium text-zinc-600">Fulfillment<select name="fulfillment" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">All</option>@foreach(['unfulfilled','partial','fulfilled','cancelled'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'fulfillment') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label><label class="text-xs font-medium text-zinc-600">More filters<select name="risk" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">Any risk</option>@foreach(['normal','low','medium','high'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'risk') === $status)>{{ str($status)->headline() }} risk</option>@endforeach</select></label><details class="text-xs text-zinc-600"><summary class="mt-3 cursor-pointer font-medium">Advanced</summary><div class="mt-2 flex flex-wrap gap-2"><label>Shipment<select name="shipment" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">All</option>@foreach(['pending','label_purchased','in_transit','delivered','failure','voided'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'shipment') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label><label>Source<select name="source" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">All</option>@foreach(['native','staff_draft'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'source') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label><label>Review<select name="review" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">All</option>@foreach(['none','pending','approved','declined'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'review') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label><label>Exception<select name="exception" class="mt-1 block rounded-md border-zinc-300 text-sm"><option value="">All</option>@foreach(['none','open','resolved'] as $status)<option value="{{ $status }}" @selected(data_get($filters ?? [], 'exception') === $status)>{{ str($status)->headline() }}</option>@endforeach</select></label></div></details><button class="fb-btn fb-btn-secondary" type="submit">Apply</button></form><span class="text-xs text-zinc-500">{{ $orders->total() }} orders</span></x-ui.operational-toolbar>
                    <div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="border-b border-zinc-200 bg-zinc-50 text-xs font-medium text-zinc-500"><tr><th class="px-4 py-3">Order</th><th class="px-4 py-3">Date</th><th class="px-4 py-3">Customer</th><th class="px-4 py-3">Payment</th><th class="px-4 py-3">Fulfillment</th><th class="px-4 py-3">Review</th><th class="px-4 py-3 text-right">Total</th></tr></thead><tbody class="divide-y divide-zinc-100">@forelse($orders as $order)<tr class="hover:bg-zinc-50"><td class="px-4 py-3"><a class="font-medium text-emerald-800 hover:underline" href="{{ route('managed-website.orders.show', $order) }}">{{ $order->number }}</a><span class="block text-xs text-zinc-500">{{ str($order->order_status)->headline() }} · {{ str($order->source)->headline() }}</span></td><td class="px-4 py-3 text-zinc-700">{{ $order->created_at->format('M j, Y') }}</td><td class="px-4 py-3 text-zinc-700">{{ data_get($order->customer_snapshot, 'name', '—') }}<span class="block text-xs text-zinc-500">{{ data_get($order->customer_snapshot, 'email') }}</span></td><td class="px-4 py-3"><x-ui.status-badge :tone="$order->payment_status === 'paid' ? 'success' : 'neutral'">{{ str($order->payment_status)->headline() }}</x-ui.status-badge></td><td class="px-4 py-3"><x-ui.status-badge :tone="$order->fulfillment_status === 'fulfilled' ? 'success' : 'warning'">{{ str($order->fulfillment_status)->headline() }}</x-ui.status-badge><span class="ml-1 text-xs text-zinc-500">{{ str($order->fulfillment_method)->headline() }}</span></td><td class="px-4 py-3"><span class="text-xs text-zinc-700">{{ str($order->risk_status)->headline() }} risk</span><span class="block text-xs text-zinc-500">{{ str($order->review_status)->headline() }} review@if($order->exception_status !== 'none') · {{ str($order->exception_status)->headline() }} exception@endif</span></td><td class="px-4 py-3 text-right font-medium text-zinc-900">${{ number_format($order->total_cents / 100, 2) }}</td></tr>@empty<tr><td colspan="7"><x-ui.operational-empty-state title="No native Website orders" description="Draft orders and confirmed Website checkout orders appear here. Shopify orders remain in Shopify." action-label="Create draft order" :action-href="route('managed-website.orders.create')" /></td></tr>@endforelse</tbody></table></div>
                    <div class="border-t border-zinc-200 px-4 py-3">{{ $orders->links() }}</div>
                </section>
            @endif
        </div>
    </flux:main>
</x-layouts::app.sidebar>
