<x-layouts::app.sidebar title="Website Leads">
    <flux:main><div class="mx-auto max-w-5xl space-y-6 pb-12"><header><a class="text-sm font-semibold text-emerald-800 hover:underline" href="{{ route('managed-website.index') }}">← Website</a><h1 class="mt-3 text-3xl font-bold tracking-tight text-zinc-950">Website Leads</h1><p class="mt-2 text-sm text-zinc-600">Quote requests and contact messages stay in this workspace. Nothing is sent automatically in this pilot.</p></header><section class="overflow-hidden border border-zinc-200 bg-white shadow-sm"><div class="divide-y divide-zinc-100">@forelse($submissions as $submission)<article class="p-5"><div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-[.14em] text-emerald-800">{{ $submission->source === 'managed_website_quote' ? 'Quote request' : 'Contact message' }}</p><h2 class="mt-1 text-lg font-bold text-zinc-950">{{ $submission->submitter_name }}</h2><p class="text-sm text-zinc-600">{{ $submission->submitter_email }} · {{ $submission->submitter_phone }}</p></div><time class="text-xs text-zinc-500">{{ $submission->submitted_at?->format('M j, Y g:i A') }}</time></div>@if($submission->source === 'managed_website_quote')<dl class="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt class="font-semibold text-zinc-500">Service address</dt><dd>{{ data_get($submission->payload, 'service_address') }}</dd></div><div><dt class="font-semibold text-zinc-500">Service needed</dt><dd>{{ data_get($submission->payload, 'service_needed') }}</dd></div></dl>@endif<p class="mt-4 text-sm leading-6 text-zinc-700">{{ data_get($submission->payload, 'message') }}</p></article>@empty<div class="p-12 text-center text-sm text-zinc-500">Quote requests and contact messages will appear here.</div>@endforelse</div></section>{{ $submissions->links() }}</div></flux:main>
</x-layouts::app.sidebar>
