<x-layouts::app.sidebar title="Collections"><flux:main><div class="mx-auto max-w-6xl space-y-5 pb-12">
@include('managed-website.catalog.nav')
<header class="flex items-center justify-between gap-4"><div><h1 class="text-3xl font-bold">Collections</h1><p class="mt-2 text-sm text-zinc-500">Group related products and manage where they appear.</p></div><a class="fb-btn fb-btn-primary" href="{{ route('managed-website.collections.create') }}">Create collection</a></header>
<section class="overflow-hidden rounded-xl border bg-white shadow-sm"><form class="border-b p-4"><label class="text-sm font-semibold">Search collections<input type="search" name="q" value="{{ $q }}" class="ml-3 rounded-lg border-zinc-300 text-sm"></label><button class="fb-btn fb-btn-secondary ml-2">Search</button></form><table class="w-full text-left text-sm"><thead class="border-b bg-zinc-50 text-xs text-zinc-500"><tr><th class="p-4">Collection</th><th class="p-4">Products</th><th class="p-4">Status</th><th class="p-4"></th></tr></thead><tbody class="divide-y">@forelse($collections as $collection)<tr><td class="p-4"><a class="flex items-center gap-4 font-semibold" href="{{ route('managed-website.collections.edit',$collection) }}">@if($collection->image_url)<img class="h-14 w-14 rounded-lg border object-cover" src="{{ $collection->image_url }}" alt="{{ $collection->title }}">@else<span class="flex h-14 w-14 items-center justify-center rounded-lg border bg-zinc-50 text-xl text-zinc-400">▦</span>@endif{{ $collection->title }}</a></td><td class="p-4">{{ $collection->products_count }}</td><td class="p-4">{{ ucfirst($collection->status) }}</td><td class="p-4 text-right"><a class="font-semibold text-emerald-800" href="{{ route('managed-website.collections.edit',$collection) }}">Edit</a></td></tr>@empty<tr><td colspan="4" class="p-12 text-center text-zinc-500">No collections yet. Create one and choose its products.</td></tr>@endforelse</tbody></table><div class="border-t p-4">{{ $collections->links() }}</div></section>
</div></flux:main></x-layouts::app.sidebar>
