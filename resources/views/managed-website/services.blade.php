<x-layouts::app.sidebar title="Services">
    <flux:main><div class="mx-auto max-w-5xl space-y-6 pb-12"><header class="flex flex-col gap-3 border-b border-zinc-200 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><a class="text-sm font-semibold text-emerald-800 hover:underline" href="{{ route('managed-website.index') }}">← Website</a><h1 class="mt-3 text-3xl font-bold tracking-tight text-zinc-950">Electrical services</h1><p class="mt-2 text-sm text-zinc-600">These are quote-only services. Customers cannot check out, pay a deposit, or create an order.</p></div><a class="fb-btn fb-btn-secondary" href="#new-service">Add service</a></header>@if(session('status'))<div class="border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-950">{{ session('status') }}</div>@endif
        <section class="divide-y divide-zinc-100 border border-zinc-200 bg-white shadow-sm">@forelse($products as $product)<article class="p-5"><div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-[.14em] text-emerald-800">Request a quote · {{ $product->status }}</p><h2 class="mt-1 text-lg font-bold text-zinc-950">{{ $product->title }}</h2><p class="mt-2 max-w-2xl text-sm leading-6 text-zinc-600">{{ $product->description }}</p></div><details class="shrink-0"><summary class="cursor-pointer text-sm font-bold text-emerald-800">Edit</summary><form method="POST" action="{{ route('managed-website.services.update', ['product' => $product]) }}" class="mt-3 grid min-w-72 gap-2">@csrf @method('PUT')<input required name="title" value="{{ $product->title }}" class="rounded border-zinc-300 text-sm"><textarea name="description" class="rounded border-zinc-300 text-sm" rows="3">{{ $product->description }}</textarea><select name="status" class="rounded border-zinc-300 text-sm"><option value="draft" @selected($product->status === 'draft')>Draft</option><option value="active" @selected($product->status === 'active')>Ready for quote requests</option></select><button class="fb-btn fb-btn-secondary justify-center" type="submit">Save</button></form></details></div></article>@empty<div class="p-12 text-center text-sm text-zinc-500">Add the electrical services visitors should be able to request.</div>@endforelse</section>
        <section id="new-service" class="border border-zinc-200 bg-white p-6 shadow-sm"><h2 class="text-xl font-bold text-zinc-950">Add an electrical service</h2><form method="POST" action="{{ route('managed-website.services.store') }}" class="mt-5 grid gap-4">@csrf<label class="text-sm font-semibold">Service name<input required name="title" placeholder="Panel upgrade" class="mt-1 block w-full rounded border-zinc-300"></label><label class="text-sm font-semibold">Clear description<textarea name="description" rows="3" placeholder="Tell customers when they should request this work." class="mt-1 block w-full rounded border-zinc-300"></textarea></label><label class="text-sm font-semibold">Availability<select name="status" class="mt-1 block w-full rounded border-zinc-300"><option value="draft">Save as draft</option><option value="active">Ready for quote requests</option></select></label><button class="fb-btn fb-btn-primary justify-center" type="submit">Save service</button></form></section>{{ $products->links() }}</div></flux:main>
</x-layouts::app.sidebar>
