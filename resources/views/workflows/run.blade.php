<x-layouts::app :title="'Workflow run #'.$run->id">
    <div class="min-h-full bg-stone-50"><div class="mx-auto max-w-4xl space-y-6 px-4 py-7 sm:px-6 lg:px-8">
        <a href="{{ route('workflows.history', ['workflow' => $run->automation_workflow_id]) }}" wire:navigate class="text-sm font-bold text-zinc-600">← Back to history</a>
        <header class="rounded-3xl border border-zinc-200 bg-white p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><span class="text-xs font-black uppercase tracking-[0.2em] text-emerald-700">Run #{{ $run->id }}</span><h1 class="mt-2 text-2xl font-black text-zinc-950">{{ $run->workflow?->name }}</h1><p class="mt-2 text-sm text-zinc-500">{{ $run->created_at->format('M j, Y \a\t g:i:s A') }} · {{ str($run->mode)->headline() }}</p></div><span class="rounded-full px-3 py-1.5 text-xs font-black {{ $run->status === 'success' ? 'bg-emerald-100 text-emerald-800' : 'bg-rose-100 text-rose-800' }}">{{ str($run->status)->headline() }}</span></div>@if($run->error_summary)<div class="mt-5 rounded-2xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-900"><strong class="block">What needs attention</strong><span class="mt-1 block">{{ $run->error_summary }}</span></div>@endif</header>
        <section class="space-y-3">@foreach($run->steps as $step)<article class="rounded-2xl border border-zinc-200 bg-white p-5"><div class="flex items-center justify-between gap-3"><div><span class="text-[10px] font-black uppercase tracking-[0.2em] text-zinc-400">Step {{ $step->position }} · {{ ucfirst($step->kind) }}</span><h2 class="mt-1 font-black text-zinc-950">{{ str($step->provider)->replace('_',' ')->headline() }}</h2></div><span class="rounded-full px-2.5 py-1 text-[10px] font-black {{ $step->status === 'success' ? 'bg-emerald-100 text-emerald-800' : 'bg-rose-100 text-rose-800' }}">{{ str($step->status)->headline() }}</span></div>@if($step->summary)<div class="mt-4 grid gap-2 rounded-xl bg-zinc-50 p-3 text-xs text-zinc-600 sm:grid-cols-3">@foreach($step->summary as $key=>$value)<div><span class="block font-black text-zinc-900">{{ str($key)->headline() }}</span>{{ is_scalar($value) ? $value : json_encode($value) }}</div>@endforeach</div>@endif</article>@endforeach</section>
        @if($run->status !== 'success')<form method="POST" action="{{ route('workflows.runs.retry', $run) }}">@csrf<button class="rounded-xl bg-zinc-950 px-5 py-3 text-sm font-black text-white">Retry this run safely</button></form>@endif
    </div></div>
</x-layouts::app>
